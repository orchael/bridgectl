apiVersion: v1
kind: Service
metadata:
  name: bridge-key-registry
  namespace: bridge-system
  labels:
    app: bridge-key-registry
spec:
  type: ClusterIP
  selector:
    app: bridge-key-registry
  ports:
    - name: https
      port: 8443
      targetPort: https
      protocol: TCP
